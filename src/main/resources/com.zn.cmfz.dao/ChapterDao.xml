<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zn.cmfz.dao.ChapterDao">
    <insert id="insert">
        insert  into chapter values(unix_timestamp(now()),#{title},#{size},#{duration},#{downPath},now(),#{pid})
    </insert>
</mapper>